# Core ML & HF
transformers==4.48.3
datasets==3.2.0
huggingface-hub>=0.17.0
peft==0.14.0
trl==0.14.0
bitsandbytes==0.46.0
accelerate==1.3.0

# PyTorch (CUDA wheel used in notebooks)
# Note: If you use CUDA, install the matching torch+cu wheel per PyTorch instructions.
# Example from notebooks: torch==2.5.1+cu124 (requires --index-url https://download.pytorch.org/whl/cu124)
torch==2.5.1+cu124
torchvision==0.20.1+cu124
torchaudio==2.5.1+cu124

# Utilities
python-dotenv
huggingface-hub
tqdm
matplotlib
wandb
requests==2.32.3
tokenizers
sentencepiece
